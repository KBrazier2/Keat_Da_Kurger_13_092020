<div class="container">
    <div class="row">
        <div class ="col-md-12 text-center" id="top-bar">
            <h1> Keat Da Kurger!</h1>
            <img src="/assets/images/burger.jpg" class="img-fluid" alt="burger" />
            <p><h2>Which one of Bob's burgers of the day are you salivating over?</h2></p>
        </div>
        
        <div class="col-md-3 text-center enter_section"></div>
        <div class="col-md-6 text-center enter_section">    
                <h4>Click "Order Up!" once you've entered your burger!</h4>
            <form action="/burgers/insertOne" method="POST">
                <input type="text" class="form-control" name="burger_name" value="" id="enter_text">
                <button type="submit" class="btn btn-primary" id="text-enter-button">Order Up!</button>
            </form>
        </div>
        <div class="col-md-3 text-center enter_section"></div>
        <div class ="col-md-12 text-center">
            <p><h4>When you're ready to eat your burger, just inhale it!</h4></p>
        </div>
        <div class="col-md-6 text-center" class="task">
            <p><h5>Burgers Ready to Eat</h5></p>
            <div class="row">
                {{#each burger_data}}
                    {{#unless this.inhaled}}
                    <div class="col-md-6 orderUpBurger text-center">
                        <pre>{{this.id}}. {{this.burger_name}}
                        </pre>
                    </div>
                    <div class="col-md-6 text-center">
                        <form class="inhale-form button-size">
                            <input input type="hidden" class="burger_id" type="text" value={{this.id}}><br>
                            <button type="submit" class="btn btn-primary">Inhale it!</button>
                        </form>
                    </div>
                    {{/unless}}
                {{/each}}
            </div>
        </div>
        <div class="col-md-6 text-center" class="task">
            <p><h5>Burgers Eaten</h5></p>
            {{#each burger_data}}
                {{#if this.inhaled}}
                    <input class="form-control" type="text" placeholder="{{this.id}} . {{this.burger_name}}" readonly>
                {{/if}}
            {{/each}}
        </div>
    </div>
</div>
