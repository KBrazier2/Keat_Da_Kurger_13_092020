<div class="container">
    <div class="row">
        <div class ="col-md-12 text-center" id="top-bar">
            <img src="/assets/images/burger.jpg" alt="burger" />
            <h1> Keat Da Kurger!</h1>
        </div>
        <div class="col-md-6 text-center" class="task">
            <div class="row">
                {{#each burger_data}}
                    {{#unless this.inhaled}}
                    <div class="col-md-9 text-center">
                        <pre>{{this.id}}. {{this.burger_name}}
                        </pre>
                    </div>
                    <div class="col-md-3 text-center">
                        <form class="inhale-form button-size">
                            <input input type="hidden" class="burger_id" type="text" value={{this.id}}><br>
                            <button type="submit" class="btn btn-default">Inhale it!</button>
                        </form>
                    </div>
                    {{/unless}}
                {{/each}}
            </div>
        </div>
        <div class="col-md-6 text-center" class="task">
            {{#each burger_data}}
                {{#if this.inhaled}}
                    <input class="form-control" type="text" placeholder="{{this.id}} . {{this.burger_name}}" readonly>
                {{/if}}
            {{/each}}
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-md-12 text-center enter_section">
            <form action="/burgers/insertOne" method="POST">
                <input type="text" class="form-control" name="burger_name" value="" id="enter_text">
                <button type="submit" class="btn btn-primary" id="text-enter-button">Submit</button>
            </form>
            <p>What kind of burger are you salivating over? Click "Submit" once you've entered your burger!</p>
        </div>
    </div>
</div>